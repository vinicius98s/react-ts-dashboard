import React from "react";

import { ButtonWrapper, Button, Icon } from "./styles";

const ActionButton: React.FC = () => {
  return (
    <ButtonWrapper>
      <Button>
        <Icon />
      </Button>
    </ButtonWrapper>
  );
};

export default ActionButton;
