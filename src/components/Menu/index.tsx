import React from "react";

import { MenuWrapper, MenuIcon } from "./styles";

const Menu: React.FC = () => {
  return (
    <MenuWrapper>
      <MenuIcon />
    </MenuWrapper>
  );
};

export default Menu;
